# 班主任班级管理系统 — 产品需求文档（PRD）

> **文档版本**：v1.3（2026-08-28）
> **文档用途**：本文档是交给 Cursor（AI 编程工具）的唯一需求来源，开发过程以本文档为准。需求变更时先改文档、再改代码。
> **产品代号**：ClassPilot（班级领航员）

---

## 目录

1. [产品概述](#1-产品概述)
2. [总体架构与技术栈](#2-总体架构与技术栈)
3. [全局技术约定](#3-全局技术约定)
4. [数据库设计](#4-数据库设计)
5. [模块详细需求](#5-模块详细需求)
6. [页面清单与路由设计](#6-页面清单与路由设计)
7. [非功能需求](#7-非功能需求)
8. [部署方案（腾讯云 2C4G）](#8-部署方案)
9. [分期路线图与验收标准](#9-分期路线图与验收标准)
10. [附录 A：.cursorrules 完整内容](#附录-acursorrules-完整内容)
11. [附录 B：内置种子数据](#附录-b内置种子数据)
12. [附录 C：上线部署 Checklist](#附录-c上线部署-checklist)
13. [附录 D：设计约定（Design Tokens）](#附录-d设计约定design-tokens与原型页验收)

---

## 1. 产品概述

### 1.1 背景

用户家属（下称"班主任"或"用户"）自 2026 年 9 月起担任初中班主任，接手一个**成绩偏下**的班级（学校分档中较差的一档），从初一带到初三毕业，为期三年。班级约 40-50 人，其中存在若干**特殊学生**：智力发育迟缓、情绪控制力差、家庭情况特殊（离异/单亲/留守/经济困难等）、身体疾病等。

班主任为非技术用户，唯一使用者，常用设备为办公室/家庭电脑。系统目标：**三年周期内显著降低班级管理的重复劳动，把碎片信息沉淀为结构化数据，并用 AI 放大单人管理效率。**

### 1.2 产品定位与设计哲学

一句话定位：**一个班主任的"单人驾驶舱"——每天 3 分钟打开系统知道今天该找谁，期末 1 小时写完全班评语，三年数据锁死在加密与备份里。**

三条设计红线，所有功能设计不得违背：

| # | 红线 | 含义 |
|---|------|------|
| R1 | **录入效率优先** | 任何录入操作必须在"课堂刚结束、只有课间 10 分钟"的场景下可完成。宁可功能少，不可录入累 |
| R2 | **隐私是红线** | 学生敏感数据（疾病/家庭/心理）加密存储、二次验证查看、导出默认脱敏、全程审计 |
| R3 | **AI 可失败，核心不可失败** | 所有 AI 功能异步、可降级、绝不阻塞核心 CRUD；DeepSeek 服务不可用时系统照常使用 |

### 1.3 用户画像与核心场景

**单一用户**：班主任本人。单账号、单角色（admin）。不设多教师协作、不设家长端（如未来需要，作为 v2 规划，本期不做）。

六个高频场景（按每天/每周期出现频率排序），每个场景对应模块验收标准：

| 场景 | 频率 | 对应模块 | 验收标尺 |
|------|------|----------|----------|
| S1 课间快速记事 | 每天 2-5 次 | 事件记录-速记模式 | 10 秒内完成一条含学生姓名的速记 |
| S2 每天打开看"今天该干嘛" | 每天 1 次 | 数据看板-首页 | 3 分钟内浏览完关注名单+待办+简报 |
| S3 考后批量登分 | 每 2-4 周一次 | 成绩录入 | 45 人 × 6 科，纯键盘操作 15 分钟内完成且零丢失 |
| S4 和家长/学生谈话前备课 | 每周 2-5 次 | 学生详情-成长时间线 | 1 分钟内调出该生三年任意维度记录 |
| S5 期中/期末写评语 | 每学期 2 次 | AI 中心-评语生成 | 全班评语从"逐个手写"变为"逐个确认"，总耗时 ≤ 2 小时 |
| S6 遇到问题查方法/政策 | 每周 1-3 次 | 知识库问答 | 提问后 30 秒内获得带来源的可信回答 |
| S7 用自然语言问班级/个人学情 | 每周数次 | AI 中心-学情问答 | 30 秒内得到带数据出处的回答（可核对到考试/名次/次数），禁止编造分数 |

### 1.4 三年数据规模预估（性能设计依据）

- 学生：50 人（含转出入变动 < 80 条记录）
- 考试：每学期 8-12 次 × 6 学期 ≈ 60 次；成绩明细 ≈ 60 × 50 × 8 科 ≈ 24,000 条
- 事件：每天 0-5 条 × 600 天 ≈ 3,000 条（含速记草稿）
- 知识库：< 300 篇文档，切段后 < 3,000 段
- 附件照片：每条事件 0-3 张，总计 < 10,000 张（约 2-5 GB）

**结论**：全库数据量级 < 100 MB，SQLite 完全胜任，无需任何缓存/队列/分库设计。所有"大数据量"的架构组件（Redis、消息队列、微服务、Elasticsearch）一律**禁止引入**。

---

## 2. 总体架构与技术栈

### 2.1 架构图

```
┌──────────────┐   HTTPS(443)   ┌──────────────────┐
│   浏览器      │ ─────────────> │  Nginx            │
│  Vue3 SPA    │                │  ├─ /        → 静态文件 dist/
│  (电脑端 only)│                │  └─ /api/*  → 反代 NestJS:3000
└──────────────┘                └────────┬─────────┘
                                         │
                                ┌────────▼─────────┐
                                │  NestJS (pm2 守护) │
                                │  ├─ better-sqlite3 │
                                │  ├─ @nestjs/schedule(定时备份)
                                │  └─ DeepSeek SDK(后端代理)
                                └────────┬─────────┘
                                         │
                        ┌────────────────┼────────────────┐
                        │                │                │
                  data/classpilot.db  uploads/        backups/
                  (SQLite 文件)     (附件原图+缩略图)  (日/周/月滚动备份)
```

### 2.2 技术栈清单（固定，Cursor 不得擅自替换）

| 层 | 选型 | 版本约束 | 说明 |
|----|------|----------|------|
| 前端框架 | Vue | 3.5+ | Composition API + `<script setup>` |
| 前端语言 | TypeScript | ≥5.6（用户所写"TS 6"按当前最新稳定版理解） | strict 全开 |
| 构建 | Vite | 8.x | |
| 状态 | Pinia | 4.x | |
| 路由 | Vue Router | 4.x | |
| 图表 | ECharts | 6.x | **按需引入**（tree-shaking，禁止全量 import） |
| UI 组件库 | Element Plus | 最新稳定 | 表格/表单/弹窗/日期等 |
| 后端框架 | NestJS | 最新稳定 11.x | |
| 数据库 | SQLite + better-sqlite3 | | 同步 API，**不用 TypeORM/Prisma**，手写 SQL + Repository 层 |
| 中文分词 | nodejieba | | FTS5 索引预分词（仅知识库模块） |
| 文档/表格解析 | mammoth(docx) / pdf-parse(pdf) / SheetJS(xlsx) | | 知识库导入；成绩 Excel 文件导入 |
| 图片处理 | sharp | | 附件缩略图 |
| 定时任务 | @nestjs/schedule | | 每日备份 |
| 进程守护 | pm2 | | 开机自启 |
| AI | DeepSeek API（openai 兼容 SDK） | model: `deepseek-chat` | **仅后端调用**，API Key 存服务器环境变量 |

### 2.3 关键架构决策记录（ADR 摘要）

| 决策点 | 结论 | 理由 |
|--------|------|------|
| ORM | 不用 TypeORM/Prisma，手写 SQL + Repository | 表少（18 张）、查询可控、Cursor 生成手写 SQL 的正确率更高 |
| SQLite 驱动 | better-sqlite3 | 同步 API 简单可靠，原生支持在线 backup 与事务 |
| 全库加密 vs 字段加密 | L2 敏感字段 AES-256-GCM 应用层加密 | SQLCipher + better-sqlite3 需自行编译，运维成本高；红线字段少且明确 |
| embedding RAG | FTS5 + jieba 关键词检索 + LLM 生成 | DeepSeek 无 embedding 接口；< 300 篇文档场景关键词检索已够用；预留 embedding 列平滑升级 |
| 学情问答 vs 知识库问答 | 分两入口：知识库查文档（§5.4）；学情问答查库内结构化数据（§5.6.5） | 数据源与防编造约束不同；禁止混在同一页同一检索链路 |
| 学情问答实现 | **禁止** LLM 直接生成/执行 SQL；规则组装数据上下文 + LLM 作答 | 单人单库可控、可审计；避免幻觉 SQL 与越权读 L2 |
| 家校沟通独立表？ | 复用 incidents 表（category=家校沟通） | 时间线聚合与统计天然覆盖，少一套页面 |
| monorepo | 单仓库双目录（backend/ + frontend/） | Cursor 对两个独立标准工程的上下文理解最稳 |
| AI 调用位置 | 仅后端 ai 模块 | 前端永不接触 API Key；统一计费与记录 |
| 图片存储 | 本地磁盘 uploads/，DB 存元数据 | 学生照片是隐私，禁用任何第三方图床/CDN |
| 成绩导入 | 粘贴（M0）+ Excel 文件导入（M1，SheetJS 后端解析）双通道 | 粘贴零依赖适合单科补录；文件导入一次灌全表，考后登分 15 分钟 → 2 分钟；统一走同一预览组件复用错位防护 |

---

## 3. 全局技术约定

### 3.1 API 风格

- REST，前缀 `/api/v1/`，资源名复数。例：`GET /api/v1/students/:id/scores?examId=12`
- **统一响应包**：

```jsonc
// 成功
{ "code": 0, "message": "ok", "data": { /* 任意结构 */ } }
// 失败
{ "code": 2001, "message": "PIN 码错误，剩余 2 次机会", "data": null }
```

- 错误码分段：`0` 成功；`1xxx` 参数校验；`2xxx` 认证/权限（2001 PIN 错误、2002 登录过期、2003 账号锁定）；`3xxx` 业务（3001 唯一冲突、3002 状态非法）；`5xxx` 系统/AI（5001 DeepSeek 超时、5002 DeepSeek 限流）
- 分页：请求 `?page=1&pageSize=50`，响应 data 结构 `{ "items": [...], "total": 123 }`
- 时间：一律 ISO 8601 **UTC** 字符串传输（`2026-09-01T08:30:00Z`），前端本地化为东八区展示。数据库列统一存 UTC ISO 字符串（SQLite 无原生时间类型，统一字符串避免歧义）
- DTO：后端 class-validator 装饰器校验；前端 api 层函数签名带 TS 类型

### 3.2 认证与安全机制

| 机制 | 规格 |
|------|------|
| 登录 | POST `/api/v1/auth/login`（username + password）→ 验证后签发 HttpOnly Cookie（JWT，7 天有效）。个人电脑场景，不做强制短会话 |
| 密码存储 | bcrypt，cost 10 |
| PIN 码 | 独立 6 位数字 PIN（bcrypt 哈希存储），仅用于解锁 L2 高敏字段。与登录密码完全独立 |
| 锁定 | 登录连续 5 次失败锁定 15 分钟；PIN 连续 5 次失败锁定 15 分钟 |
| CSRF | SameSite=Strict Cookie + 双提交 token |
| 路由守卫 | 所有 `/api/v1/**` 需认证中间件（健康检查 `/api/health` 除外） |
| L2 解锁缓存 | PIN 验证通过后，服务端在会话内记录"敏感解锁至 timestamp"，**10 分钟有效**，过期需重输。期间可连续查看多条 L2 记录，每次查看均写审计 |

### 3.3 数据安全分级（贯穿所有模块）

| 级别 | 内容示例 | 存储方式 | 访问控制 | 导出策略 |
|------|----------|----------|----------|----------|
| L0 公开 | 姓名、学号、性别 | 明文 | 登录后可见 | 可导出 |
| L1 内部 | 成绩、普通标签、事件记录、监护人联系方式 | 明文 | 登录后可见 | 可导出 |
| L2 高敏 | 疾病史、心理评估、家庭变故详情、家暴记录 | **AES-256-GCM 加密**（student_sensitive 表） | 登录 + PIN 解锁 + 审计 | **默认永不导出**；导出界面提供显式勾选开关（勾选时二次确认+审计） |

**审计日志**（audit_logs）：每次 L2 查看、每次导出、每次 PIN 尝试失败均记录 `{action, target_student_id, detail, created_at}`。审计日志只增不删，提供"安全日志"页面查看。

### 3.4 前端工程约定

- **UI 组件库：Element Plus**（固定）。所有表格/表单/弹窗/菜单/标签/空状态等基础交互**必须**使用 Element Plus 组件（`el-table`、`el-form`、`el-menu`、`el-card`、`el-tag`、`el-empty` 等），禁止自研替代组件。`@element-plus/icons-vue` 为唯一图标库
- **设计令牌层**：`styles/tokens.css` 通过 CSS 变量覆盖 Element Plus 主题色与全局字号/间距，`.cp-*` 类仅为布局辅助（卡片容器、页面标题区等），**不是**第二套 UI 库
- 布局固定 `min-width: 1200px`，**不做响应式/移动端适配**（唯一例外：速记弹窗等自有交互不依赖视口宽度）
- 视觉规范遵循**附录 D 设计令牌**：`styles/tokens.css` 为颜色/间距/圆角/阴影全局唯一定义处，组件内禁止硬编码色值；ECharts 调色板与业务域色一致（附录 D.5）
- 侧边导航使用 **`el-menu` + `router` 模式**，选中态样式见附录 D.3
- ECharts 封装为 `<VChart>` 通用组件（接收 option + 自动 resize + 按需注册）
- axios 实例统一拦截：解包 `{code,message,data}`、401 跳登录、错误 toast（ElMessage）
- 所有列表页统一分页/筛选参数由 Pinia store 持有，返回页面恢复状态
- 表格录入、时间线、灯箱、速记窗为全局复用组件，放 `components/`，禁止在页面内重复实现
- 页面切换与卡片 hover 使用附录 D.9 微动画规范，禁止夸张动效

---

## 4. 数据库设计

> 共 18 张表。全表软删除（`deleted_at`）。SQLite 主键统一 `INTEGER PRIMARY KEY AUTOINCREMENT`。
> 迁移机制：`backend/migrations/` 下编号 SQL 脚本（`001_init.sql`、`002_xxx.sql`），服务启动时自动按序执行未应用脚本，执行记录存 `migrations` 表。**不使用第三方迁移库。**

```sql
-- 001_init.sql 全量建表

-- 账号（单用户，预置一条）
CREATE TABLE users (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  username TEXT NOT NULL UNIQUE,
  password_hash TEXT NOT NULL,
  pin_hash TEXT,                    -- L2 解锁 PIN，bcrypt
  display_name TEXT,                -- "X老师"
  failed_attempts INTEGER DEFAULT 0,
  locked_until TEXT,
  created_at TEXT NOT NULL,
  updated_at TEXT
);

-- 学期（预置 6 学期，可手动续加）
CREATE TABLE terms (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL UNIQUE,        -- "2026-2027 第一学期"
  start_date TEXT, end_date TEXT,
  grade INTEGER                     -- 1/2/3 年级
);

-- 科目（可配置：初一语数英+道法+地生，初二加物理，初三加化学）
CREATE TABLE subjects (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  code TEXT NOT NULL UNIQUE,        -- yu/shu/wai/...
  name TEXT NOT NULL,               -- 语文/数学/...
  full_score REAL NOT NULL,         -- 满分（120/100/70…）
  grade_start INTEGER DEFAULT 1,    -- 从几年级开课
  sort INTEGER DEFAULT 0,
  enabled INTEGER DEFAULT 1,
  deleted_at TEXT
);

-- 学生
CREATE TABLE students (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  student_no TEXT NOT NULL UNIQUE,  -- 学号
  name TEXT NOT NULL,
  gender INTEGER,                   -- 0 女 1 男
  birth_date TEXT,
  photo_path TEXT,
  ethnicity TEXT, address TEXT, residence TEXT,
  enrolled_at TEXT,                 -- 入学时间
  status TEXT DEFAULT '在读',       -- 在读/转出/休学/毕业
  board_type TEXT,                  -- 走读/住宿
  cadre_role TEXT,                  -- 班干部职务，空为非班干部
  focus_level INTEGER DEFAULT 0,    -- 关注等级 0-3（0普通 3最高）
  remark TEXT,
  created_at TEXT NOT NULL, updated_at TEXT, deleted_at TEXT
);

-- L2 高敏明细（内容 AES-256-GCM 加密）
CREATE TABLE student_sensitive (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  student_id INTEGER NOT NULL REFERENCES students(id),
  category TEXT NOT NULL,           -- 健康/心理/家庭/其他
  content_encrypted BLOB NOT NULL,  -- 密文
  iv BLOB NOT NULL,                 -- 每条随机 IV
  updated_at TEXT,
  deleted_at TEXT
);

-- 审计日志（只增不删）
CREATE TABLE audit_logs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  action TEXT NOT NULL,             -- view_sensitive / export / pin_failed / unlock_sensitive
  target_student_id INTEGER,
  detail TEXT,                      -- JSON：{"fields":"家庭情况","count":1}
  created_at TEXT NOT NULL
);

-- 标签字典
CREATE TABLE tags (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  domain TEXT NOT NULL,             -- 学业/行为情绪/健康/家庭/特长/其他
  name TEXT NOT NULL,
  color TEXT,                       -- 前端标签色
  sensitive_level INTEGER DEFAULT 0,-- 0 普通 / 1 半敏感（如"父母离异"摘要级）/ 2 高敏(禁止出现在聚合视图)
  is_builtin INTEGER DEFAULT 0,
  deleted_at TEXT,
  UNIQUE(domain, name)
);
CREATE TABLE student_tags (
  student_id INTEGER NOT NULL REFERENCES students(id),
  tag_id INTEGER NOT NULL REFERENCES tags(id),
  PRIMARY KEY (student_id, tag_id)
);

-- 监护人（一生多条）
CREATE TABLE guardians (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  student_id INTEGER NOT NULL REFERENCES students(id),
  relation TEXT, name TEXT, phone TEXT, wechat TEXT, job TEXT,
  contact_pref TEXT,                -- 电话/微信/面谈（沟通偏好）
  best_time TEXT,                   -- "工作日18点后"
  is_primary INTEGER DEFAULT 0,
  remark TEXT, deleted_at TEXT
);

-- 考试
CREATE TABLE exams (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,               -- "第一次月考"
  exam_type TEXT,                   -- 单元测/月考/期中/期末/模考/其他
  term_id INTEGER REFERENCES terms(id),
  exam_date TEXT,
  subject_ids TEXT NOT NULL,        -- JSON [1,2,5]
  grade_ref TEXT,                   -- JSON 年级参考：{"subjectId":{avg,pass}}，可选
  status TEXT DEFAULT '未录入',     -- 未录入/录入中/已发布/已归档
  deleted_at TEXT
);

-- 成绩（排名物化）
CREATE TABLE scores (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  exam_id INTEGER NOT NULL REFERENCES exams(id),
  student_id INTEGER NOT NULL REFERENCES students(id),
  subject_id INTEGER NOT NULL REFERENCES subjects(id),
  score REAL,                       -- null=未录
  status TEXT DEFAULT '正常',       -- 正常/缺考/免考
  class_rank INTEGER,               -- 班排（物化，重算覆盖）
  UNIQUE (exam_id, student_id, subject_id)
);
CREATE INDEX idx_scores_student ON scores(student_id);

-- 事件记录（含速记草稿 + 家校沟通）
CREATE TABLE incidents (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  occurred_at TEXT NOT NULL,        -- 事件发生时间
  category TEXT NOT NULL,           -- 纪律违纪/情绪行为/伤病健康/家校沟通/表扬奖励/学习问题/其他
  severity INTEGER DEFAULT 1,      -- 1轻 2中 3重
  title TEXT,
  content TEXT,                     -- 规范正文（确认后）
  draft_content TEXT,               -- 原始速记（永久保留）
  ai_suggestion TEXT,               -- 已废弃：原 AI 整理快照，不再写入
  status TEXT DEFAULT 'draft',      -- draft(速记草稿)/confirmed(已确认)
  follow_up_needed INTEGER DEFAULT 0,
  follow_up_deadline TEXT,
  follow_up_done_at TEXT,
  created_at TEXT NOT NULL, updated_at TEXT, deleted_at TEXT
);
-- 涉事学生多对多
CREATE TABLE incident_students (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  incident_id INTEGER NOT NULL REFERENCES incidents(id),
  student_id INTEGER NOT NULL REFERENCES students(id),
  is_primary INTEGER DEFAULT 1,
  role_note TEXT                     -- "挑起者/被动卷入"
);

-- 附件
CREATE TABLE attachments (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  incident_id INTEGER REFERENCES incidents(id),  -- 预留其他模块引用扩展
  file_path TEXT NOT NULL, thumb_path TEXT,
  mime TEXT, size INTEGER, sha1 TEXT,
  created_at TEXT
);

-- 知识库
CREATE TABLE kb_documents (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  title TEXT NOT NULL,
  category_path TEXT,               -- "政策法规/义务教育"
  source TEXT,                      -- 出处/网址
  file_path TEXT,                   -- 原文件（可选）
  content_text TEXT,                -- 抽取的全文
  seg_count INTEGER DEFAULT 0,
  created_at TEXT, deleted_at TEXT
);
CREATE TABLE kb_segments (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  document_id INTEGER NOT NULL REFERENCES kb_documents(id),
  seq INTEGER NOT NULL,
  text TEXT NOT NULL,               -- 原文段落
  tokenized_text TEXT NOT NULL,     -- jieba 分词后空格序列（喂给 FTS5）
  embedding TEXT                    -- 预留列，本期恒为 null
);
-- FTS5 虚表
CREATE VIRTUAL TABLE kb_segments_fts USING fts5(
  tokenized_text, text UNINDEXED, segment_id UNINDEXED
);

-- AI Prompt 模板
CREATE TABLE ai_prompts (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  scene TEXT NOT NULL,              -- comment/report/talk_script/work_summary/data_qa/kb_qa(仅 records)；incident_extract 已取消
  name TEXT NOT NULL,
  template TEXT NOT NULL,           -- 含 {{占位符}}
  style_params TEXT,                -- JSON：{tone,length,...}
  is_builtin INTEGER DEFAULT 0,
  is_default INTEGER DEFAULT 0,
  deleted_at TEXT
);

-- AI 调用记录
CREATE TABLE ai_records (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  scene TEXT NOT NULL, prompt_id INTEGER,
  student_id INTEGER,               -- 可空（报告类可能面向全班）
  context_snapshot TEXT,            -- JSON：实际注入的上下文
  output_text TEXT,
  model TEXT, tokens_in INTEGER, tokens_out INTEGER,
  status TEXT DEFAULT 'generated',  -- generated/adopted/edited_adopted/discarded
  created_at TEXT
);

-- 评语（AI 生成后采纳落库 / 手写）
CREATE TABLE comments (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  student_id INTEGER NOT NULL REFERENCES students(id),
  term_id INTEGER REFERENCES terms(id),
  comment_type TEXT,                -- 期中/期末/毕业
  final_text TEXT NOT NULL,
  source_ai_record_id INTEGER,      -- 来源 AI 记录，可空=纯手写
  created_at TEXT, deleted_at TEXT
);

-- 系统设置（KV）
CREATE TABLE settings (
  key TEXT PRIMARY KEY,
  value TEXT                        -- JSON
);

-- 迁移记录
CREATE TABLE migrations (
  name TEXT PRIMARY KEY, applied_at TEXT NOT NULL
);
```

### 4.1 物化视图/派生数据口径（写死，避免歧义）

| 口径 | 规则 |
|------|------|
| 班排 | 按分数降序，并列同名次（如两人并列第 3，则下一名是第 5）；缺考/免考**不参与**排名 |
| 总分/总排 | 各科 score 求和后同规则排名；某科缺考则总分排名中该生标"不完整"，仍参与（用现有科目和） |
| 低分 | 单科分数 < 满分 × 40%（阈值存 settings，可改） |
| 及格 | ≥ 满分 × 60%；优秀 ≥ 满分 × 85%（均可配置） |
| 进步/退步 | 相邻两次**同类型大考**（期中/期末/模考）班排变化 ≥ +8 名为显著进步、≤ −8 名为显著退步（阈值可配置） |
| 等第制 | ABC 只在展示层按当前阈值转换，**不存库**（原始分永远是唯一事实） |

---

## 5. 模块详细需求

### 5.0 认证与账号（auth）

- 登录页：用户名 + 密码。首次部署通过 CLI 命令创建账号（`npm run cli:create-user`）
- PIN 管理：设置页可设置/修改 PIN（需先验登录密码）
- 登出、修改密码
- 前端路由守卫：未登录一律重定向 /login

### 5.1 学生档案（students）

#### 5.1.1 功能清单

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 花名册列表 | 默认表格：学号/姓名/性别/**班干部**/关注等级/标签(脱敏展示)/状态；支持按姓名学号搜索、按关注等级/状态筛选；列可配置。**不展示就读方式**（本班目前均为走读、不上夜自习） | M0 |
| 学生新增/编辑 | 普通字段表单；照片上传（可选） | M0 |
| **花名册导入** | Excel 粘贴式导入（同成绩粘贴方案）：粘贴姓名列或姓名+学号两列 → 匹配/新建 | M0 |
| 特殊学生三层体系 | 见 5.1.2 | M0（标签+关注等级）/ M1（加密明细） |
| 监护人管理 | 一生多条，主联系人标记 | M0 |
| 成长时间线 | 聚合视图，见 5.1.3 | M1 |
| L2 高敏明细 | 加密存取 + PIN 解锁 + 审计，见 5.1.2 | M1 |
| 回收站 | 软删除恢复 | M2 |

#### 5.1.2 特殊学生三层体系（本模块核心设计）

**第一层 · 关注等级（focus_level 0-3）**：一个整数字段，看板"重点关注名单"、首页卡片墙都由它驱动。设计意图：班主任每天第一眼的信息不是成绩，是"谁今天可能出事"。

**第二层 · 标签字典（tags）**：按域分组的短标签，`sensitive_level` 决定展示策略：

- `sensitive_level=0`（普通）：如"体育特长""作业拖拉"，任何聚合视图可见
- `sensitive_level=1`（半敏感）：如"父母离异""疑似多动"，仅在学生详情页显示，**聚合视图/导出物中隐藏**
- `sensitive_level=2`（高敏）：仅作为"存在 L2 明细"的入口提示（如🔒图标），内容本身永不进标签

**第三层 · L2 高敏明细（student_sensitive 表）**：

- 类别：健康 / 心理 / 家庭 / 其他，每类一条长文本
- AES-256-GCM 加密，密钥来自 `data/.key`（部署时生成，权限 600，**不进 git、不上传 COS 备份**）
- 查看交互流：学生详情页 → 点击🔒高敏卡片 → 弹窗输 PIN → 后端验 PIN + 会话内 10 分钟解锁 → 返回解密内容 + 写审计 → 超时后重新弹 PIN
- 编辑：解锁状态下可直接编辑保存（重新加密），编辑也写审计

#### 5.1.3 成长时间线（学生详情页"时间线"Tab）

- 数据来源：该学生的 `scores`（考试节点）+ `incidents`（事件，含家校沟通）+ `comments`（评语），按时间倒序合并
- 布局：左侧垂直时间轴 + 右侧卡片；卡片按类型着色（成绩蓝/事件橙/沟通绿/评语紫/表扬金）
- 筛选：按类型 / 学期 / 关键词
- 用途标尺（验收）：家长会面谈前 1 分钟调出该生三年任意记录（S4 场景）

#### 5.1.4 学生详情页结构

```
头部：照片 | 姓名 学号 班干部 | 关注等级选择器(0-3) | 状态
Tab1 档案：基本信息 + 监护人卡片(含沟通偏好/最佳时段) + 标签编辑
Tab2 高敏：🔒 加密卡片 × (健康/心理/家庭/其他) → PIN 解锁
Tab3 成绩：单科/总分趋势折线(分数+班排双轴) + 最近一次大考雷达图(vs 班均分) + 明细表
Tab4 时间线：成长时间线(5.1.3)
Tab5 评语：历次评语列表 + 新建(可走 AI)
Tab6 我的印象：班主任主观印象长文本（课堂观察、同学/科任看法等）；保存后注入学情问答（单生范围）与评语生成上下文，供当前及后续 AI 分析
```

> **敏感边界**：印象为 L0/L1 工作笔记，**禁止**写入 L2 高敏诊断/家暴等明文；高敏内容仍走「高敏」Tab + PIN。
### 5.2 成绩管理（scores）

#### 5.2.1 基础配置

- **学期管理**：预置 6 学期，可改起止日期
- **科目管理**：启用/停用、满分、开课年级。三年科目变化（初一加地生、初二加物理、初三加化学）靠它支撑，**写死科目即返工**
- **考试管理**：创建考试 = 名称/类型/学期/日期/勾选参考科目（各科可单独覆盖满分，如体育 70）；可选填"年级参考数据"（各科年级均分/及格线，班主任拿到年级 Excel 后手动粘贴导入，用于班 vs 年级对比）

#### 5.2.2 成绩录入（本模块核心，交互规格精确到按键）

**模式选择：逐科录入（推荐默认）**——一次录一科。理由：列数少不横向滚动、可并排显示"上次该科成绩"做对照、Excel 粘贴一列即对齐。

**录入页布局**（全屏模式，遮蔽侧边栏防误触）：

```
顶栏：考试名 | 科目切换 Tab | 进度"已录 32/45" | [粘贴导入] [Excel 导入] [保存并重算排名] [完成退出]
表格列：学号 | 姓名 | 上次该科成绩(灰) | 本次成绩(可编辑) | 状态(正常/缺考/免考)
```

**键盘交互（验收按此清单逐条测试）**：

| 按键 | 行为 |
|------|------|
| `Enter` / `↓` | 焦点移到下一行同列 |
| `↑` | 焦点移到上一行同列 |
| 输入数字 | 直接覆盖当前格 |
| `Tab` | 浏览器默认（下一格） |
| `X` | 标记缺考（格子显示"缺"，灰色，不计统计） |
| `M` | 标记免考 |
| `Esc` | 清空当前格回到未录入 |
| 越界值（>满分或<0） | 即时红框 + 顶部提示，不阻断继续录入 |

**保存策略**：

1. 每格失焦触发，前端聚合 800ms 防抖内的变更，批量 `PATCH /api/v1/scores/batch`
2. 同时写 localStorage（key 含 examId+subjectId），页面刷新/误关后进入录入页时检测到暂存 → 提示"恢复上次未保存的 N 条"
3. 离开录入页（含浏览器刷新/关闭，用 beforeunload）弹确认
4. **"保存并重算排名"**：后端事务内全量重算该考试该科目班排写入 `scores.class_rank`（数据量 50 行 < 100ms，无需增量算法）
5. 考试状态机：未录入 →（开始录入）录入中 →（重算+确认）已发布 →（期末后）已归档

**导入方式（两种并存，各有场景）**：

方式一：**粘贴导入**（M0）——零依赖、日常补录单科用：

```
点击[粘贴导入] → 弹窗 textarea → 从 Excel 复制一列分数粘贴
→ 前端按行解析 → 与当前学生列表按顺序对齐 → 预览表（每行：学生名 | 解析值 | 状态✓/⚠）
→ 错位防护：若某行解析失败或值越界，该行标红，其余可提交；提供"按学号列对齐"模式（粘贴两列：学号+分数）
→ 确认后批量写入
```

方式二：**Excel 文件导入**（M1）——考后一次性灌全表用，45 人 × 8 科从 15 分钟压到 2 分钟：

```
入口：考试详情页 [Excel 导入] 或录入页顶栏
→ 上传 .xlsx（后端 SheetJS 解析，前端不做解析，文件不上传到磁盘只在内存处理）
→ 模板：提供"下载导入模板"按钮，生成含当前班级学生名单（学号+姓名行）+
        该考试参考科目列的空表；也支持导入非模板表（列名按科目名模糊匹配）
→ 解析后进入统一预览表（与粘贴导入共用同一预览组件）：
    每行 = 学号 | 姓名 | 各科解析值 | 状态（✓ 匹配 / ⚠ 学号不存在 / ⚠ 值越界 / ⚠ 姓名与学号不符）
    支持"覆盖已有成绩 / 仅填空格"两种写入模式（考试录入中途补导时不破坏已录数据）
→ 错误行默认跳过、可单行修正后重试；确认后事务内批量写入
→ 特殊值约定：单元格为 "缺" / "X" → 缺考；"免" / "M" → 免考；空 → 不写入
```

约束：单文件 ≤ 2MB、仅 .xlsx（.xls 提示转存）；导入完成自动提示"是否立即重算排名"。


#### 5.2.3 成绩分析

- **单生视图**（学生详情 Tab3）：单科/总分趋势折线（X=考试，Y1=分数，Y2=班排）；最近一次大考单科雷达 vs 班均分；相邻大考进退步标签
- **班级视图**：
  - 历次大考班均总分趋势折线 + 年级均分参考虚线（有 grade_ref 才画，图例注明"年级参考"）
  - 各科低分率/及格率/优秀率堆叠柱（**低分率排序为默认视图**——差班第一指标）
  - 各科分数分布直方图
  - 进步/退步榜（横向条形，±8 名）
- **成绩单导出**：HTML 打印样式 + CSV 下载（仅 L0/L1 数据）

### 5.3 事件记录（incidents）

#### 5.3.1 数据结构要点

- **涉事学生多对多**：一次冲突/打闹事件涉及多人是常态。主学生（is_primary=1）+ 关联学生，每人可带角色备注（"挑起者/被动卷入"）
- **家校沟通复用本表**：category=家校沟通，content 中记录沟通对象/方式/结果。时间线与统计天然覆盖，不另建表
- **跟进机制**：`follow_up_needed` + `follow_up_deadline`；到期未完成 → 看板首页待办区红点 + 事件列表高亮

#### 5.3.2 速记模式（S1 场景，验收：10 秒记一条）

- 全局快捷键 `Alt+Q`（任意页面）唤起**悬浮速记窗**（居中小窗，不离开当前页面上下文）
- 窗内仅三个元素：**学生选择器**（输入姓名首字母/拼音即时过滤，支持多选）+ **自由文本框** + **类别快速选择**（默认"其他"）
- `Ctrl+Enter` 保存 → 存为 `status='draft'` 草稿 → 主界面顶部"待整理：N 条"角标
- **语音输入**：文本框旁麦克风按钮，Web Speech API（Chrome/Edge 中文识别免费可用）。不可用环境自动隐藏按钮
- 速记窗支持连续录入（保存后自动清空、焦点回到学生选择器）——课堂课间连记多条的场景

#### 5.3.3 事件整理（手工确认，**已取消 AI 整理**）

> **2026-08-28 产品确认**：不做「AI 整理 / `incident_extract`」闭环。速记草稿仅走**手工结构化确认**入库；`draft_content` 仍永久保留原文；`ai_suggestion` 列保留为空兼容，不再写入。

流程：

```
[速记草稿池] → 打开草稿 → 手工填写类别/严重度/涉事学生/标题正文/跟进
     → 确认 → status='confirmed'
```

- 草稿永不因任何 AI 不可用而丢失（本功能本身不依赖 AI）
- 模板管理中不再提供 `incident_extract` 场景新建；历史内置若有则只读或隐藏

#### 5.3.4 事件列表与时间线

- 列表页：过滤器（学生/类别/严重度/时间范围/仅看待跟进/仅看草稿）；默认倒序
- 卡片展示：类别色条 + 标题 + 涉事学生 chips + 严重度星标 + 跟进状态
- 单筛选某学生 = 该生"事件侧档案"，与成绩侧拼成完整学生页
- 附件：每条事件可上传照片（≤10MB/张，仅图片与 PDF），后端 sharp 生成 300px 缩略图，查看用灯箱，下载原图仅登录会话内（`/api/v1/attachments/:id/file` 走认证）

### 5.4 知识库（knowledge，M2）

#### 5.4.1 文档管理

- 上传 txt / md / docx / pdf / xlsx（含 .xls）；后端抽纯文本（docx→mammoth，pdf→pdf-parse，xlsx→SheetJS，失败则提示改用"粘贴文本"入口）
- 目录树分类（政策法规/特殊教育/心理干预/班会素材/家校沟通话术/其他，可自定义）+ 标签
- **切段**：按标题/空行切成 300-500 字段，存 kb_segments

#### 5.4.2 轻量 RAG 问答（无 embedding 的实现方案，写死技术路线）

```
用户提问 → nodejieba 切词 → SQLite FTS5 MATCH（tokenized_text 双写方案）
→ BM25 取 top 6~8 段 → 拼上下文（每段前缀"[文档标题 · 第N段]"，总长 ≤ 3000 字）
→ DeepSeek（system prompt 硬约束）：
    "仅依据给定资料回答；每个论点后标注来源文档名；
     资料未覆盖时明确回答'知识库中未找到相关内容'，禁止编造；
     涉及学生具体处置时附加提醒'建议结合本校规定与学生实际情况判断'"
→ 前端：回答 + 命中原文段落折叠卡片（可点开逐段核对）
```

- 中文 FTS5 关键实现：**jieba 预分词双写**——入库时把 segment 切词成空格序列写入 `tokenized_text`（FTS5 索引列，原文另存普通列）；查询时对输入同样切词后拼 MATCH 串
- 问答历史保存（复用 ai_records，scene=kb_qa）
- 升级预留：kb_segments.embedding 列已存在，检索层做成 Strategy 接口，未来接 embedding 时替换实现不改表

### 5.5 数据看板（dashboard）

**原则：每张图回答班主任一个具体问题，不堆图。** 默认首页 = "今天做什么"，全量图表放"分析中心"二级页。

#### 首页（登录后落地页）

> **2026-08-28 UI 确认变更**：③ 成绩简报区块**暂时移除**，展示方案待用户下次确认后再开发。M1 前首页仅保留两区块；成绩相关图表统一放「分析中心」或后续确认的入口。

| 区块 | 内容 | 数据口径 |
|------|------|----------|
| ① 重点关注卡片墙 | focus_level ≥ 2 的学生卡片：姓名+照片+关注原因标签(L0/L1)+最近一条事件摘要+最近一次家校沟通距今天数 | "今天该找谁"；沟通距今 > 21 天的卡片标橙色边框 |
| ② 待办跟进 | follow_up_deadline ≤ 今天且未完成的事件列表（红点）；速记草稿"待整理 N 条"入口 | "别忘了承诺过的回访" |
| ~~③ 成绩简报~~ | ~~最近一次已发布大考的迷你趋势线 + 各科均分/低分率一览表~~ | **暂缓，待 UI 确认** |

#### 分析中心图表清单（8 张）

| # | 图表 | ECharts 类型 | 回答的问题 | 分期 |
|---|------|--------------|-----------|------|
| 1 | 班级总分趋势 | line + markLine（年级参考） | 班整体在追吗 | M1 |
| 2 | 各科低分率/及格率 | 堆叠 bar | 哪科要抓 | M1 |
| 3 | 进步/退步榜 | 横向 bar | 谁要表扬/谈话 | M1 |
| 4 | 关注频率分布 | bar（近 30 天每生事件+沟通次数，降序） | 关注是否失衡（只盯问题生漏了沉默大多数） | **M2 第一批** |
| 5 | 家校沟通日历热力 | calendar heatmap | 沟通节奏，家长会前补短板 | **M2 第一批** |
| 6 | 事件类别分布 | pie/donut（本学期） | 班风主要矛盾 | M2 |
| 7 | 单科分布直方图 | bar | 分数段结构 | M2 |
| 8 | 长期留存：事件月度趋势 | line（3 年） | 班风变化纵向对比 | M2 |

图表 4/5 是"管理视角"差异化价值所在：差班班主任容易陷入"只处理尖叫的学生"，这两张图用数据暴露盲区。

**M2 第一批（本批交付）另含学情问答次要入口**：分析中心顶栏「用自然语言问」→ `/ai/ask`；学生详情「问该生学情」→ `/ai/ask?studentId=`。

### 5.6 AI 中心（ai）

#### 5.6.1 Prompt 模板管理

- 模板 = 场景 × 可编辑模板正文 × 风格参数。场景枚举：`comment`（评语）/ `report`（综合素质报告）/ `talk_script`（沟通话术）/ `work_summary`（学期工作总结）/ `data_qa`（学情问答）/ `kb_qa`（知识库问答，仅 records）。~~`incident_extract` 已取消~~
- 模板正文 `{{占位符}}` 系统：编辑器侧栏列出全部可用占位符 + 当前示例值（防止写出系统拼不出的变量）
- 内置模板（is_builtin=1）可克隆不可删；每场景一个默认模板；评语内置 2 个风格（温婉型/严谨型）
- 风格参数：语气（亲切/朴实/严肃）、长度（短/中/长）、是否含建议项

#### 5.6.2 评语生成（P0，上下文注入是质量关键）

生成单个学生评语时，后端**自动组装**上下文（规则拼接，不用 LLM 做摘要预处理，事件量大时先跑一次廉价压缩）：

```
[学生档案] 姓名、性别、学期、L0/L1 级标签
[成绩摘要] 最近 2 次大考各科分数+班排、较上次进退、本学期最强/最弱科目（规则拼接）
[事件摘要] 本学期事件按类别聚合：表扬类保留原文全量；
           违纪/情绪类仅保留 AI 压缩后的一句话摘要（事件>10 条时先批量压缩再聚合）
[上次评语] 上学期末评语全文（防重复、体现成长连贯性）
[班主任印象] 学生详情「我的印象」长文本（截断注入；禁止当 L2 高敏用）
[风格] 模板 style_params 渲染
```

**Token 预算硬约束**：注入上下文 ≤ 2500 tokens；超限时按"违纪摘要→次要事件→成绩明细"顺序逐级丢弃（保留严重度最高的）。

#### 5.6.3 批量评语工作台（S5 场景，验收：全班 ≤ 2 小时）

```
选择学期+评语类型+模板 → 左侧全班学生列表（状态：未生成/已生成/已采纳 ✓）
→ 逐个生成（也可"全部生成"排队串行，前端进度条，单条失败不中断）
→ 右侧分栏：左边 AI 输出（可编辑），右边"引用数据"折叠卡片（注入了哪些成绩/事件，可核对）
→ [采纳] 写入 comments 表并标记 ai_record status=adopted
→ 同一学生可反复"重新生成"；历史版本可回看（ai_records 列表）
→ 全部采纳后 [批量导出]（Word/HTML，按学号排序，L2 数据永不出现）
```

- 生成失败重试 2 次（指数退避），仍失败标记该生"生成失败"可单条重试，**不阻塞工作台其他操作**
- 编辑中的评语自动暂存 localStorage 防丢失

#### 5.6.4 其他 AI 场景

- **事件整理**：仅手工确认（**已取消 AI 整理**，见 5.3.3）
- **沟通话术（talk_script）**：输入场景描述（如"小张妈妈来电投诉被同桌欺负，对方家长不认"）+ 可选关联学生 → 生成沟通策略草稿。**上下文可注入该生档案与相关事件摘要**（勾选开关，默认开）
- **学期工作总结（work_summary）**：注入本学期班级聚合数据（考试均分趋势、事件统计、沟通次数、表扬榜）→ 生成班主任工作总结初稿（**整篇一次性**；与学情问答的"问一句答一句"不同）
- **知识库问答**：见 5.4.2（查上传文档，不是查成绩库）
- **学情问答（data_qa）**：见 5.6.5

#### 5.6.5 学情问答（data_qa，**M1**，S7）

> **定位**：用自然语言查询**本班已入库的结构化学情**（成绩、事件、关注、沟通频次等），回答必须可回溯到系统数据。  
> **明确不做**：不替代分析中心图表；不与知识库 RAG 共用检索；不生成可执行 SQL；不注入 L2 高敏明文。

##### M1 交付范围（本期必做）

- 主路由 `/ai/ask` + 侧栏「学情问答」；支持范围：**全班** / **指定学生**（下拉或 `?studentId=`）
- 能回答成绩进退、班排、低分/及格相关、事件类别次数、关注生名单、家校沟通偏少等常见问法（见验收样例）
- 回答 +「引用数据」折叠卡；写 `ai_records`（`scene=data_qa`）
- DeepSeek 不可用时：仍返回规则拼接的数据摘要 + 提示
- **M1 可不做**：分析中心/学生详情次要入口可后补；服务端多轮记忆不做；`includeLastComment` 默认关且 UI 可不暴露

##### 典型问法（验收样例）

| 范围 | 示例问题 | 期望回答要点 |
|------|----------|--------------|
| 全班 | 「上次期中谁进步最大？数学低分率多少？」 | 点名学生 + 名次变化/低分率数字 + 标注考试名称 |
| 全班 | 「最近三次考试数学连续进步的有哪些同学？」 | 规则统计近 3 场数学分严格递增名单 + 分值轨迹；不足 3 场则明确说明 |
| 全班 | 「这学期家校沟通偏少的学生有哪些？」 | 按沟通次数排序的名单 + 距今天数口径说明 |
| 个人 | 「张三最近两次大考各科班排怎么变的？」 | 两次考试对照表意文字 + 科目进退 |
| 个人 | 「李四本学期违纪和表扬各几次？」 | 按事件类别计数；可点开核对（前端给关联入口） |
| 边界 | 「王五有没有抑郁症诊断？」 | **拒绝展开 L2**；提示去学生详情「高敏」Tab（需 PIN），回答中不得出现密文解密内容 |
| 无数据 | 「物理竞赛成绩怎么样？」 | 明确「系统数据中未找到相关记录」，禁止编造 |

##### 技术路线（写死）

```
用户提问 + 范围(全班 | 指定学生Id)
  → [可选] DeepSeek JSON 意图抽取：scope / studentNameHints / examHints /
            metrics[](score_rank|avg|low_rate|progress|incidents|focus|contact) / termHint
     （抽取失败则走规则兜底：姓名匹配花名册、关键词映射指标）
  → 后端按白名单**规则组装**数据上下文（手写 SQL / 现有 Service，**禁止 LLM 写 SQL**）
  → 拼装 system prompt 硬约束 + 数据块（学情问答专用预算：**≤ 约 12000 tokens / 18000 字**；优先注入紧凑成绩台账）
  → DeepSeek 生成自然语言回答
  → 前端：回答 +「引用数据」折叠卡（展示注入了哪些考试/统计行，便于核对）
  → 写 ai_records（scene=data_qa，student_id 可空=全班）
```

**数据上下文白名单（可注入）**：

- 学生：学号、姓名、性别、状态、班干部、L0/L1 标签与关注等级、**不含** L2 明文
- **成绩（放宽）**：以紧凑「成绩台账」注入尽量多年份/多场考试的各科分与总分（旧→新）；超预算时从最旧考试裁切，优先保留较新场次。问句命中「某科+连续进步」时可附带预计算名单（降低模型数错）。仍可读阈值算低分/及格口径。
- 事件：近 180 天按类别计数、最近若干条标题级摘要；家校沟通次数与最近沟通距今天数
- 评语：最近 1 条已采纳评语全文（可选，默认关，UI 开关；M1 可不暴露）
- **班主任印象**（单生范围）：`student_impressions` 文本（截断注入）；全班问答默认不批量注入以免撑爆预算

**硬约束（system prompt）**：

1. 仅依据给定数据块回答；每个关键数字/结论后标注出处，格式如（数据：{考试名} · {科目/指标}）
2. 数据块未覆盖时回答「系统数据中未找到相关记录」，禁止编造分数、名次、人次
3. 涉及处置建议时附加「建议结合本校规定与学生实际情况判断」
4. 任何心理疾病诊断、家暴、家庭变故细节等：一律不答具体内容，引导至高敏流程

**与邻近功能边界**：

| 功能 | 差异 |
|------|------|
| 知识库 `/knowledge/ask` | 查上传文档；本功能查库表 |
| 工作总结 `work_summary` | 一次生成整篇总结；本功能多轮单次问答 |
| 分析中心 | 图表浏览；本功能对话查数，可从分析页跳入并预填问题 |

##### 页面与 API

- 路由：`/ai/ask`（AI 分组导航「学情问答」）
- 次要入口（不替代主路由）：分析中心顶栏「用自然语言问」；学生详情「问该生学情」→ `/ai/ask?studentId=`
- API：`POST /api/v1/ai/data-ask`  
  Body：`{ question, studentId?, termId?, includeLastComment? }`  
  响应：`{ available, message?, answer, citations[], aiRecordId, contextText }`  
  `citations[]`：`{ kind: exam|student|incident_stat|..., label, detail }`
- 会话：本期**不做服务端多轮记忆**；页内保留本会话历史列表（仅前端），点选可回看；正式历史以 `ai_records` 为准
- 模板：`data_qa` 场景可在「模板管理」配置 system 侧说明文案；内置 1 个默认模板（可克隆）
- 降级：DeepSeek 不可用时，仍返回「引用数据」折叠卡中的结构化摘要（规则拼接短文），并提示 AI 暂不可用
- 回答展示：前端渲染 Markdown **子集**（粗体、无序列表、换行）；禁止渲染原始 HTML/链接跳转；完整 CommonMark 不引入额外依赖

#### 5.6.6 DeepSeek 接入实现约定

- OpenAI 兼容 SDK，baseUrl `https://api.deepseek.com`，model `deepseek-chat`；Key 存服务器 `.env`（`DEEPSEEK_API_KEY`），**任何代码/日志/前端响应中不得出现 Key**
- 所有调用：超时 60s；失败重试 2 次（1s/4s 退避）；结构化抽取场景 `response_format: json_object`（含学情意图抽取）
- 每次调用写 ai_records（tokens 统计、上下文快照）
- 后端提供 `GET /api/v1/ai/health`（轻量 ping + 当月 token 消耗汇总），设置页展示

### 5.7 系统设置（settings）

- 阈值配置：低分线/及格线/优秀线/进退步名次阈值（改变后展示层数据即时生效，历史存档不受影响）
- 评语模板管理入口
- 修改密码/PIN
- 备份管理页：手动触发备份、查看备份列表（文件名/大小/quick_check 结果/时间）、从备份恢复（恢复前强制二次确认 + 自动先备份当前库）
- 安全日志页（audit_logs 只读浏览）
- AI 用量统计（当月 tokens/调用次数/失败率）

---

## 6. 页面清单与路由设计

```
/login                    登录页
/                         首页看板（关注卡片墙+待办；成绩简报暂缓）
/students                 花名册列表（含班干部等非敏感列；不展示就读方式）
/students/:id             学生详情（Tab: 档案/高敏/成绩/时间线/评语）
/students/:id/edit        编辑（或抽屉式，开发时定）
/scores                   考试列表
/scores/exams/:id         考试详情（全科成绩总表，各科/总分列头排序）
/scores/exams/:id/enter   成绩录入页（全屏模式，逐科录入）
/incidents                事件列表（含草稿池 Tab）
/incidents/:id            事件详情/编辑
/knowledge                知识库文档列表
/knowledge/ask            知识库问答（查上传文档，S6）
/ai/comments              评语工作台
/ai/ask                   学情问答（查库内成绩/事件等，S7）
/ai/prompts               模板管理
/ai/records               生成历史
/analysis                 分析中心（8 张图；可跳转 /ai/ask）
/settings                 设置（Tab: 基础/安全/备份/日志/AI用量）
全局组件：速记悬浮窗（Alt+Q）、灯箱、时间线组件、表格录入器
```

---

## 7. 非功能需求

### 7.1 性能（写死约束，作为验收项）

- 单并发用户；所有列表接口 P95 < 300ms（本地 SQLite 读取，绰绰有余）
- 成绩全量重算 < 1s；首页看板聚合接口 < 500ms
- 前端首屏 JS gzip 后 < 1MB（ECharts 按需引入 + 路由懒加载 + Element Plus auto-import 按需样式）
- 附件上传限制：≤ 10MB/张，仅 `image/jpeg|png|webp` 与 `application/pdf`
- pm2 单实例即可，**禁止**引入 Redis / 消息队列 / Docker 编排 / 微服务

### 7.2 可靠性与备份（3 年数据不能丢，四层防线）

1. **软删除**：全表 deleted_at，回收站可恢复（防手滑）
2. **每日在线备份**：`@nestjs/schedule` 每日 02:30，better-sqlite3 `db.backup()`（在线一致性备份，不停服）→ gzip → `backups/daily/` 保留 30 份滚动
3. **周/月锚点**：每周日备份转存 `backups/weekly/`（留 26 份）；每月 1 号转存 `backups/monthly/`（留 36 份，覆盖 3 年）——月度锚点跨整个三年周期
4. **异地容灾**：每周备份完成后上传腾讯云 COS 私有桶（服务器被入侵/磁盘损坏时的最后防线）；README 写明"建议每月手动下载一份到家里电脑"（人是最后一层备份）

- 备份包内容：`classpilot.db` + `uploads/` 全量打包
- 每次备份后执行 `PRAGMA quick_check` 并把结果写备份日志（**备份了但损坏 = 没备份**）
- 备份目录、`data/.key`、`.env` 三者**永不**进入 git 与 COS 之外的同步渠道；`.key` 与 `.env` 不上传 COS

### 7.3 AI 可用性降级矩阵

| 故障 | 系统行为 |
|------|----------|
| DeepSeek 超时/限流 | 重试 2 次 → 提示"AI 暂不可用，稍后重试"；对应功能退化为手工模式（评语照常手写）；**学情问答**降级为仅展示规则组装的「引用数据」摘要 |
| 结构化抽取返回非法 JSON | 学情意图抽取失败则规则兜底（姓名/关键词）；~~事件 AI 整理已取消~~ |
| tokens 异常暴涨（上下文组装 bug） | 上下文截断保护（评语等 **2500~3000 tokens**；**学情问答允许约 12000 tokens**，仍须硬上限截断） |

**验收原则：拔掉 DEEPSEEK_API_KEY，系统的全部非 AI 功能 100% 可用。**

### 7.4 服务器安全加固（部署阶段执行，附录 C 有 checklist）

- SSH 密钥登录、禁密码、禁 root
- ufw 仅开放 22/80/443
- NestJS 以非 root 用户 + pm2 运行
- fail2ban（nginx 401 计数封禁）
- HTTPS：Let's Encrypt + certbot 自动续期，HTTP 301 强制跳转
- `data/`、`uploads/`、`backups/` 目录权限 700

---

## 8. 部署方案

**目标机**：腾讯云轻量服务器 2C4G / 60G SSD / 5Mbps。

**目录规划**（`/opt/classpilot/`）：

```
/opt/classpilot/
├── backend/           # NestJS 构建产物 + node_modules(仅生产依赖)
├── frontend/dist/     # Vite 构建产物（Nginx 直接托管）
├── data/
│   ├── classpilot.db
│   └── .key           # AES 密钥（600 权限，不进备份/git）
├── uploads/
├── backups/{daily,weekly,monthly}/
├── logs/              # pm2 + winston 日志，按天滚动留 90 天
└── .env               # DEEPSEEK_API_KEY 等（600 权限）
```

**Nginx 要点**：

```
server {
  listen 443 ssl http2;
  root /opt/classpilot/frontend/dist;
  location / { try_files $uri $uri/ /index.html; }      # SPA history 路由
  location /api/ { proxy_pass http://127.0.0.1:3000; }
  client_max_body_size 12m;                              # 附件上传
}
```

**发布流程（写在 README，Cursor 生成 deploy 脚本）**：本地 build → rsync/scp 上传 → pm2 reload backend → 前端 dist 覆盖即生效。提供 `deploy/deploy.sh` 一键脚本。

---

## 9. 分期路线图与验收标准

> 时间背景：文档定稿于 2026-08-27，开学在即。M0 是**应急范围**，优先保开学用上；完整体验在期中前到位。

### M0 — 开学应急版（目标：9 月中旬前上线可用）

| 交付 | 验收标准 |
|------|----------|
| tokens.css + 4 页静态骨架 | 附录 D 令牌落地；首页/成绩录入/学生详情/事件列表四个页面纯静态版（假数据），与用户共同确认布局后再填数据流 |
| 项目骨架 + 认证登录 | 前后端跑通、HTTPS 可访问、登录/登出/改密 |
| 学生档案基础版 | 花名册粘贴导入 45 人 < 5 分钟；新增/编辑/搜索/筛选可用；标签 + focus_level |
| 监护人管理 | 增删改查 + 沟通偏好字段 |
| 成绩录入基础版 | 考试/学期/科目配置 + 逐科录入 + 保存 + 重算排名 + 简单查询（键盘导航可先简化为 Enter 下移，粘贴导入 M1） |
| 事件速记（无 AI） | Alt+Q 速记窗 + 草稿池 + 手工结构化确认 + 事件列表/筛选 |
| 部署与备份 | Nginx+HTTPS+pm2 上线；每日备份任务跑通且 quick_check 通过 |

### M1 — 期中家长会前（10 月中旬）

| 交付 | 验收标准 |
|------|----------|
| 成绩录入完整版 | 键盘导航全按键清单（5.2.2 表格逐条测试）；粘贴导入含预览/对齐/错误标红；Excel 文件导入（模板下载 + 预览 + 两种写入模式）；localStorage 暂存恢复 |
| 事件速记与确认 | Alt+Q 速记 + 草稿池 + **手工**结构化确认 + 列表/筛选（**不做** AI 整理） |
| 评语生成 + 工作台 | S5 验收：全班评语 ≤ 2 小时（生成质量以"班主任只改 2-3 句"为标尺） |
| **学情问答 data_qa** | **S7 验收：30 秒内带数据出处回答；与知识库问答分入口；禁止 LLM 写 SQL；无 AI 时仍可看引用数据摘要** |
| 首页看板 | S2 验收：3 分钟看完关注墙+待办（成绩简报待后续确认方案后再加） |
| L2 高敏加密 | PIN 解锁流程 + 10 分钟缓存 + 审计日志 + 导出脱敏 |
| 成长时间线 | S4 验收：1 分钟调出该生三年任意记录 |
| 成绩分析基础图 | 趋势线/低分率/进退步榜 |

### M2 — 初一下学期 ~ 初二上

**已交付**：

- 知识库 + RAG 问答（提前）
- 分析中心图 4–8 全量 + 学情问答次要入口
- 沟通话术 `/ai/talk`、学期工作总结 `/ai/summary`
- 事件附件（认证下载 + sharp 缩略图 + 灯箱）
- 回收站（学生/事件/评语/考试/知识库文档恢复）

**仍属后续（初二专项，可按年级节奏开）**：

- 科目加物理；地生会考专项跟踪视图（进度、模拟成绩、目标线）
- 速记窗拍照上传（事件详情已支持附件；速记窗入口可后补）

### M3 — 初二下 ~ 初三

- 中考专项：目标高中/目标分设定与差距跟踪、历次模考对比
- 志愿参考资料归档（知识库）
- **毕业档案一键导出**：三年成绩全史 + 大事记 + 全部评语 → Word/PDF（默认脱敏 L2，导出勾选需 PIN + 审计）
- 三年纵向回溯报表（月度事件趋势、沟通总量、班级均分三年线）
- **原则：初三不新增重功能**（老师没时间学新工具），只做"稳定 + 出口交付物"

---

## 附录 A：.cursorrules 完整内容

> 以下内容原样放入项目根目录 `.cursorrules`：

```markdown
# ClassPilot 班主任班级管理系统 — 开发规则

## 技术栈（白名单，不得擅自替换或新增依赖；新增任何依赖必须说明理由并等用户确认）
- 前端：Vue 3.5 + <script setup> + TypeScript(strict) + Vite 8 + Pinia 4 + Vue Router 4 + ECharts 6(按需引入) + Element Plus
- 后端：NestJS + better-sqlite3（手写 SQL，禁用 TypeORM/Prisma）+ class-validator + @nestjs/schedule
- 部署：Nginx + pm2 + SQLite 文件

## 工程结构
- 单仓库双目录：backend/（NestJS 标准结构）、frontend/（Vite 标准结构）、deploy/（nginx.conf、deploy.sh、备份脚本）
- 后端分层强制：Controller（仅参数校验与路由）→ Service（业务）→ Repository（唯一允许 import better-sqlite3 写 SQL 的层）
- AI 调用只允许出现在 backend/src/modules/ai/ 的 service 中
- 敏感字段加解密只允许经 backend/src/crypto/ 的统一 helper，且每次读写必须写 audit_logs

## 代码规范
- TypeScript strict 全程开启；禁止 any（用 unknown + 类型收窄）
- 所有时间存储与传输：ISO 8601 UTC 字符串；前端展示时本地化
- API：REST /api/v1/，统一响应 { code, message, data }；错误码分段 1xxx 参数/2xxx 认证/3xxx 业务/5xxx 系统
- 数据库变更：只通过 backend/migrations/ 编号 SQL 脚本，启动时自动执行；禁止改已应用的迁移文件，只能新增
- 全表软删除（deleted_at），禁止物理删除业务数据
- 前端不做响应式适配，布局 min-width: 1200px
- UI 严格遵循 PRD 附录 D 设计令牌：颜色/间距/圆角/阴影只允许引用 tokens.css 的 --cp-* 变量或 Element 变量；禁止组件内硬编码色值；禁止暗色主题；渐变/阴影档位仅允许附录 D 白名单用途（侧栏 Logo 块、页面英雄横幅、头像点缀、页面氛围底）
- ECharts 用 components/VChart 封装，按需注册组件，禁止 import 'echarts' 全量；调色板与域色见附录 D.5
- 提交信息：Conventional Commits（feat(scores): 键盘导航录入）

## 安全红线（违反即返工）
- DeepSeek API Key 只存服务器 .env，任何日志、响应、前端代码中不得出现
- L2 高敏内容（student_sensitive）只存密文，禁止明文写库/写日志
- uploads/ 文件访问必须走认证接口，禁止 Nginx 静态直出 uploads
- 所有 /api/v1/** 接口必须过认证守卫（/api/health 除外）

## AI 约定
- AI 功能全部可失败可降级：DeepSeek 不可用时核心 CRUD 必须完全可用
- 调用超时 60s，重试 2 次（1s/4s 退避）；结构化抽取用 response_format: json_object
- 每次调用写 ai_records（tokens + 上下文快照）

## 测试
- Service 层核心路径单测（vitest）：排名重算、录入聚合保存、AI 上下文组装（token 预算截断）、加密解密往返
- 三条链路 e2e 必须有：登录、成绩录入保存重算、每日备份
- 验收场景以 PRD 1.3 节 S1-S6 为标尺

## 需求来源
- 一切以《班主任班级管理系统-需求文档.md》为准；发现文档矛盾或缺口时，先停下来询问，不要自行发挥
```

## 附录 B：内置种子数据

**默认标签（is_builtin=1，用户可停用可加）**：

| 域 | 标签（sensitive_level） |
|----|------------------------|
| 学业 | 偏科(0)、作业拖拉(0)、进步快(0)、需要基础辅导(0) |
| 行为情绪 | 情绪易波动(1)、易冲突(1)、注意力难集中(1)、自我管理弱(0) |
| 健康 | 视力问题(0)、体育锻炼受限(0)、慢性疾病(2→仅作 L2 入口提示) |
| 家庭 | 父母离异(1)、单亲(1)、留守儿童(1)、经济困难(1)、隔代抚养(1)、家中有变故(2) |
| 特长 | 体育(0)、文艺(0)、动手能力强(0) |

**内置评语 Prompt 模板示例（温婉型）**：

```
你是一位经验丰富的初中班主任，正在为 {{student_name}} 撰写{{term}}期末评语。
请基于以下真实资料，写出一段真诚、具体、有细节的评语，避免空话套话，
语气{{style_tone}}，长度{{style_length}}。

该生本学期情况：
【成绩】{{score_trend}}
【课堂与作业】{{incident_summary}}
【闪光时刻】{{praise_summary}}

要求：
1. 至少引用一个具体事例（来自上面的资料）
2. 先肯定具体的进步，再委婉指出 1 个改进方向
3. 结尾给一句个性化鼓励
4. 与上学期评语风格连贯但避免用词重复：{{last_comment}}
```

**~~事件抽取 system prompt~~**：**已取消**（见 5.3.3），不再实现。

## 附录 C：上线部署 Checklist

```
□ 服务器基础：SSH 密钥登录、禁密码、ufw 仅 22/80/443、fail2ban 安装
□ 运行环境：Node 22 LTS（或 20）、pm2 全局安装
□ 目录：/opt/classpilot/{data,uploads,backups/{daily,weekly,monthly},logs} 权限 700
□ 生成 data/.key（openssl rand -32 hex，权限 600）
□ .env 写入 DEEPSEEK_API_KEY（权限 600）
□ 执行 001_init.sql 迁移 + 创建账号（npm run cli:create-user）
□ 域名解析到服务器公网 IP
□ Nginx 配置 + certbot 签发 Let's Encrypt 证书 + 自动续期定时任务
□ pm2 启动 backend 并设置开机自启（pm2 startup + pm2 save）
□ 验证：登录可用 / 首页看板 / 速记一条事件 / 建一次考试录一次成绩
□ 验证：手动触发备份，检查 backups/daily 出现文件且 quick_check=ok
□ 腾讯云 COS 建私有桶 + 配置周备上传（SecretKey 存服务器，不进 git）
□ 保存恢复演练一次：用备份文件恢复到临时库，确认可读
```

## 附录 D：设计约定（Design Tokens 与原型页验收）

### D.0 设计基调

**淡色、有层次、品牌点缀但不浮夸。** 浅色主题为主，页面浅蓝氛围底 + 白卡片抬起阴影；侧栏 Logo 用主色渐变色块强化识别；首页等主工作页可用主色英雄横幅。对标气质：比 Notion/Linear 更「醒目一档」的现代教育工具，仍非营销落地页或深色控制台。关键词：淡色、蓝氛围、渐变点缀（白名单）、三档阴影、更大圆角、域色点缀。

所有令牌（token）集中定义在 `frontend/src/styles/tokens.css`，是全局**唯一**颜色/间距/圆角/阴影/白名单渐变定义处，组件内一律引用变量。

### D.1 色彩令牌

以仓库现行 `frontend/src/styles/tokens.css` 为准（摘要）：

- 主色 `#2563EB`，辅强调 `#0EA5E9`
- 页面底 `--cp-bg-page: #F3F7FF` + `--cp-page-atmosphere` 双径向浅蓝氛围
- 边框偏靛蓝浅色 `#C7D2FE`，卡片白底
- 阴影三档：`--cp-shadow-1` 卡片 / `--cp-shadow-2` 抬起 / `--cp-shadow-3` 英雄区
- 圆角：卡片/弹窗 16、控件 10、标签胶囊 999
- **白名单渐变**（仅这些场景）：
  - `--cp-gradient-brand`：侧栏 Logo 块
  - `--cp-gradient-hero`：页面英雄横幅（`.cp-hero`）
  - `--cp-gradient-avatar`：头像/首字点缀
  - `--cp-page-atmosphere`：内容区背景

Element Plus 主题绑定仍通过同文件 `--el-*` 变量覆盖，**禁止**散落在各组件里改主题色。

**域色标签规则**：标签一律"浅底深字"——背景 = 域色约 10% 透明度，文字 = 域色原值；可用圆角胶囊。禁止实心色标签作为默认信息标签。

### D.2 字体与排版

- 字体栈：`-apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Microsoft YaHei", "Noto Sans SC", sans-serif`
- **数字列（成绩/排名/统计）必须 `font-variant-numeric: tabular-nums`**，表格内对齐是精致感的底线
- 字号阶梯（仅 6 档，禁止其他字号）：**13**（辅助/表头）、**14**（表格正文/标签/元信息）、**15**（正文基准）、**18**（区块标题/顶栏）、**22**（页面标题）
- 行高 1.6；页面标题区 = 标题（22/700）+ 副说明（14/text-2）+ 右侧主操作按钮
- 学生详情页字号与全局一致，**不得低于正文基准 15px**；区块标题用 18px/600

### D.3 布局骨架

```
┌──────────┬──────────────────────────────┐
│ 侧边导航  │ 顶栏 56px：面包屑 | [速记 Alt+Q] | [系统设置] | 用户  │
│ 220px    ├──────────────────────────────┤
│ (可折叠   │  内容区 padding 24px           │
│  64px)   │  筛选卡 → 内容卡（页面统一模式）  │
└──────────┴──────────────────────────────┘
```

- 侧边导航可折叠至约 72px 图标模式，折叠状态存 localStorage；**使用 Element Plus `el-menu`（router 模式）**；展开态顶部为**主色渐变 Logo 色块**（ClassPilot + 副标题）；选中项：浅蓝渐变底 + 主色文字/图标 + **左侧居中短竖条（3×18px 圆角）**；折叠态仅保留浅蓝底高亮；导航分组：工作台 / 学生 / 成绩 / 事件 / 知识库 / AI（含评语工作台、**学情问答**、模板、生成历史）/ 分析；**系统设置入口在顶栏右侧、用户名左侧**
- min-width 1200px；内容区 > 1440px 时卡片栅格自动多列，**不设内容最大宽度**；内容区背景使用 `--cp-page-atmosphere`
- 页面统一结构：**（可选）英雄横幅 `.cp-hero` → 筛选条（白卡）→ 内容卡**；首页看板使用英雄横幅
- 卡片基础样式：`bg --cp-bg-card + 1px --cp-border + radius 16 + shadow-1`；可点击卡片 hover 升 shadow-2 并上移约 4px
- 每页 primary 主操作按钮 ≤ 1 个，其余 default / text；danger 操作必须二次确认

### D.4 组件密度规范

| 组件 | 规格 |
|------|------|
| el-table | 表头背景 #F1F5F9、**14px/600**、左对齐（操作列居中）；正文 **15px**；**成绩数字列 18px/600**（`--cp-font-md`）；行高 40（默认）/**48（成绩相关页）**；关斑马纹；行分割线 --cp-divider |
| el-form | label 左对齐，统一 label-width 88px；项间垂直间距 16px |
| el-tag | 高约 22、12–13px、胶囊圆角、浅底深字（见 D.1 域色规则） |
| el-button | 主操作 primary、次操作 default、行内操作 text；同一视觉层级不并排放多个 primary |
| 弹窗 | radius 12、标题 16/600；宽度：480（表单）/ 720（导入预览类）/ 自适应（灯箱） |
| 空状态 | 统一内置 SVG 插画位 + 一句话说明 + 主操作按钮；禁止裸文本"暂无数据" |
| 加载 | 首屏骨架屏（el-skeleton），列表局部 loading 用 el-loading 遮罩；禁止整页白屏等待 |

### D.5 图表规范（与域色强一致）

- VChart 默认调色板：`['#3D6FE8', '#ED8B33', '#33A675', '#8B5CF6', '#D4A017', '#8F959E']`
- 网格线仅横向 #F0F1F3，纵向无；轴线隐藏；非数据文本 12px #646A73
- 折线：smooth 0.3、线宽 2、数据点仅 hover 显示；面积图填充为系列色 10% 透明度
- 柱状：barWidth ≤ 32、圆角 [4,4,0,0]、无背景柱
- tooltip：白底、1px #E5E6EB、shadow-2、radius 8、13px
- 进步/正向用 --cp-success，退步/预警用 --cp-danger（本系统无行情场景，不用红涨绿跌）
- 动画仅初次渲染 600ms；**禁止** 3D、炫光、大面积渐变填充

### D.6 图标与质感原则

- 图标统一 `@element-plus/icons-vue` 线性图标；禁止 emoji 充当功能图标
- 质感原则：**淡色氛围底 + 白卡片抬起**；阴影用 D.1 三档；**渐变仅白名单**（Logo 块 / 英雄横幅 / 头像点缀 / 页面氛围）；禁止全站深色主题与毛玻璃大面积滥用

### D.7 四个原型页验收要点（M0 静态骨架阶段，与用户爱人共同确认）

1. **首页看板**：顶部 **`.cp-hero` 主色横幅** + **两区块**（关注卡片墙 → 待办跟进）；~~成绩简报暂缓~~；关注卡片 4 列栅格、整卡可点击进学生详情、沟通超 21 天的卡片带橙边+浅橙渐变底；待办列表 ≤ 5 条 + "查看全部"；待办红点可波动画；使用 `el-card`/`el-tag`/`el-empty`
2. **成绩录入页**：全屏模式无侧边导航；顶栏（考试名/科目 Tab/进度/操作）吸顶；表格占满剩余高度、内容滚动而表头 sticky；**当前焦点格 2px --cp-primary 外框**；已录/未录（--cp-divider 底）/缺考/免考四种格子状态一眼可区分
3. **学生详情页**：头部信息条（照片 64px 圆角方形 + 姓名/学号/班干部 + focus_level 控件）+ Tab 白卡区（`el-tabs`/`el-card`/`el-descriptions`）；时间线左轴线 2px --cp-divider、节点圆点 10px 按域色着色、卡片随类型带 5px 左侧域色条
4. **事件列表页**：筛选条单行；卡片 2 列栅格（>1600px 时 3 列）；每卡结构：5px 左侧类别色条 + 标题(15/600) + 学生 chips + 底部元信息行(14/text-2：时间 · 严重度 · 跟进状态)
5. **花名册列表页**（补充）：表格展示班干部等非敏感列；**不展示就读方式**（全班走读）；筛选含关注等级/状态；使用 `el-table`/`el-tag`/`el-space`
6. **考试详情页**（补充）：`/scores/exams/:id` 全科成绩同页表格查看（学号/姓名/各科/总分/总排）；**各科列与总分列支持点击列头升序/降序排序**（未录/缺考/免考始终沉底）

### D.8 禁止清单（硬约束，违反即返工）

- 禁止组件内硬编码色值/阴影/圆角/渐变（必须引用 `tokens.css` 变量或 Element 默认）
- 禁止暗色主题；禁止在白名单之外新增渐变用途或阴影档位
- 禁止引入 Tailwind/UnoCSS 或第二套 UI 库
- 禁止混用图标风格、emoji 图标、过度卡通化造型
- 禁止装饰性摄影大图背景；英雄横幅仅限工作页信息引导，不做营销轮播

### D.9 微动画与 UI 确认变更记录（2026-08-28）

**微动画规范**（精致、克制，禁止夸张）：

| 场景 | 效果 | 参数 |
|------|------|------|
| 页面区块进入 | 淡入 + 轻微上移 | `cp-animate-in`，0.4s，`cubic-bezier(0.4,0,0.2,1)` |
| 路由切换 | 淡出淡入 | `cp-fade`，0.25s |
| 可点击卡片 hover | 上移约 4px + 阴影升档 | transform + shadow，0.2s |
| 待办红点 | 双环波动 | `cp-pulse-dot`，约 2.4s |
| Tab 指示条 | 平滑滑动 | Element Plus 默认 + 0.25s |
| 表格行 hover | 背景色过渡 | 0.15s |

**用户 UI 确认变更摘要**（静态骨架阶段）：

| 变更项 | 结论 |
|--------|------|
| 字号 | 全局放大一档（见 D.2）；学生详情同步加大 |
| 配色 | 主色/功能色饱和度提高；边框偏靛蓝浅色（见 tokens.css） |
| 视觉方向 | 对比档2/档3后**选定淡色档3**：氛围底 + Logo 渐变色块 + 首页英雄横幅 + 三档阴影 + 更大圆角；**不做深色模式** |
| 侧边菜单选中态 | `el-menu`；浅蓝渐变底 + 主色文字 + 左侧短竖条；折叠态图标居中 |
| 微动画 | 按上表落地；待办红点可波动画 |
| 花名册 | 增加班干部列；**不展示就读方式** |
| 首页成绩简报 | **移除**，展示方案下次再确认 |
| 考试详情 | 全科同页查看 + 各科/总分列头排序 |
| UI 框架 | **Element Plus 为唯一 UI 库**；`.cp-*` 为设计令牌辅助类 |
| 预览页 | `/ui-preview` 仅对比用，确认后可保留或后续删除 |
| 学情问答（2026-08-28） | 新增 S7；落点 AI 中心 `/ai/ask`（§5.6.5）；与知识库问答分入口；**提前至 M1 交付**；禁止 LLM 写 SQL；M2 再补次要入口 |
| 学情问答上下文放宽 | 以紧凑成绩台账为主注入多场考试各科分（预算约 1.2 万 tokens）；兼容连续进步等开放问法，而非仅固定摘要指标 |
| 系统设置入口 | 从侧栏移至**顶栏右侧、用户名左侧** |
| 取消事件 AI 整理（2026-08-28） | 不做 `incident_extract`；草稿仅手工确认 |
| M2 第一批（2026-08-28） | 分析图 4–5 + 学情问答次要入口 |
| M2 收尾（2026-08-28） | 图 6–8、话术/工作总结、回收站、事件附件；初二专项仍后置 |

---

*文档结束。开发中发现的任何需求疑问，以本文档 1.2 三条设计红线（R1 录入效率 / R2 隐私 / R3 AI 可降级）为最高裁决标准。*
